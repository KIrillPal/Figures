/*typedef struct BoundXZ
{
	float x, y, width, height;
	BoundXZ(float x, float y, float w, float h)
		: x(x), y(y), height(h), width(w) {};
};
bool isWindowOpen();
bool pollEvent(sf::Event e);
void DrawGraphWindow(BoundXZ real_window, BoundXZ mind_window);*/